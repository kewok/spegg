//
// Include your mySpecies_genotype_phenotype_maps header file here
//

GenotypePhenotypeMap *GenotypePhenotypeMap::create_genotype_phenotype_map(inds *species, int phenotype_index, int index_case, int num_kids)
	{
/*
	* A generic factory for the code that checks for the phenotypes required, and then returns the genotype-phenotype map for the phenotype of the species as specified by the user.
	*/

   // test for whether phenotype_index == species->demeParameters->species_specific_values['phenotype_name'])
   // 	return new this_traits_genotype_phenotype_map_object(species, index_case, number_of_kids);
	}
